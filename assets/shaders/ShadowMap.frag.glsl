#version 450

// Shadow map fragment shader
// No output needed - depth is written automatically to the depth attachment

void main() {
    // Empty fragment shader
    // Depth testing and writing happens automatically
    // Could add alpha testing here if needed for vegetation/transparent objects
}
